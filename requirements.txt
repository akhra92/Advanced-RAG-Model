# Streamlit Cloud resolves torch from the CPU-only index; the default PyPI wheel
# bundles CUDA and blows past the memory and disk budget.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.40
python-dotenv>=1.0
chromadb>=1.0
huggingface_hub>=0.34
sentence-transformers>=3.0
torch>=2.2
pydantic>=2.0
tenacity>=9.0
tqdm>=4.66
